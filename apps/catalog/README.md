# apps/catalog/

Empty by design. Not yet implemented — see `apps/README.md`.

A future addition here must satisfy the same contract as everything else under `apps/`: fully
optional, deletable without affecting the platform (T1), and addable without a platform change
(T2). CI checks both on every pull request.
