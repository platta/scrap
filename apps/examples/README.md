# apps/examples/

Not yet implemented. See `apps/README.md` for the six patterns this directory will demonstrate, and
the repository root `README.md` roadmap for sequencing — examples land after `platform/` and before
`capabilities/`, since they are what proves the platform's application contract actually holds
before anything optional is layered on.
