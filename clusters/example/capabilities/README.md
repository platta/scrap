# clusters/example/capabilities/

Empty. This **is** the `minimal` profile — no capability `Kustomization` files are present, so no
optional capability reconciles. See `clusters/README.md` for how presence enables a capability, and
`capabilities/README.md` for what's available to add.
