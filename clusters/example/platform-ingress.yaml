# dependsOn platform-cert-manager: this Kustomization applies a Certificate
# (wildcard-certificate.yaml) referencing the scrap-ca ClusterIssuer that
# platform-cert-manager owns. tests/assertions/check_kustomization_dag.py
# proves this dependency is actually declared, not merely convenient --
# the direct fix for a real class of bug where a Certificate was applied
# before its issuer was guaranteed to exist.
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: platform-ingress
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./platform/ingress
  prune: true
  sourceRef:
    kind: GitRepository
    name: scrap
  dependsOn:
    - name: platform-crds
    - name: platform-cert-manager
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: instance-config
  wait: true
  timeout: 5m0s
