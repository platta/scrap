# Only the actual Secret manifests are Kustomize resources here --
# .sops.yaml, README.md, and the reference age key sitting in this same
# directory are not listed, so Kustomize ignores them.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - restic-credentials.sops.yaml
