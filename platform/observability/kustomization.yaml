apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
# The golden-path PodMonitor + baseline PrometheusRule live in the
# SEPARATE platform/observability-config/ directory, applied by its own
# Flux Kustomization with dependsOn + wait: true on this one. Same real
# ordering bug as platform/cert-manager-config/ -- see that directory's
# kustomization.yaml.
