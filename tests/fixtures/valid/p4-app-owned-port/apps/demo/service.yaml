# Part of the same positive fixture as this directory's reserved-ports.yaml:
# a LoadBalancer Service whose port is authorized purely by the app's own
# declaration, no platform/ present.
apiVersion: v1
kind: Service
metadata:
  name: demo
  namespace: demo
spec:
  type: LoadBalancer
  ports:
    - port: 6000
      protocol: TCP
