apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: scrap-wildcard
  namespace: traefik
spec:
  secretName: scrap-wildcard-tls
  dnsNames:
    - "*.example.internal"
  issuerRef:
    name: ${TLS_ISSUER}
    kind: ClusterIssuer
    group: cert-manager.io
