# VIOLATION: this Kustomization applies a Certificate referencing an issuer
# owned by cert-manager-config.yaml, but does NOT dependsOn it.
# check_kustomization_dag.py must catch this ordering bug.
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ingress
spec:
  path: ./platform/ingress
